name: d3_sealed
description: A Dart 3-style sealed class builder.
version: 1.0.0

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  build: ^2.3.1
  build_config: ^1.1.1
  source_gen: ^1.2.7
  sealed_annotations: any
  flutter:
    sdk: flutter
  meta: ^1.8.0

dev_dependencies:
  build_runner: ^2.3.3
  build_test: ^2.1.7
